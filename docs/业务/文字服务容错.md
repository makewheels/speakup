# 文字服务容错

评估、标准答案、出题、翻译和追问共享文字服务切换机制。
主服务无法响应时，系统按配置顺序尝试两级备用，用户无需重新选择服务。
服务失败后会短暂跳过它，之后自动恢复优先尝试主服务。

追问已出现半截回答而上游中断时，当前回答会清空，并由下一家重新生成。
成功时历史仅保存最终完整回答。全部服务不可用时仍显示失败提示，可稍后重试。
标准答案和反馈独立生成，单个分支失败仍保留另一分支的可用内容。

只有配置完整并且接口实际可用的备用服务才能提供容错。
