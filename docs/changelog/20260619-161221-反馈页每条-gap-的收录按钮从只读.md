# 反馈页每条 gap 的收录按钮从只读「Saved」改成可点切换——`+ Add to Review` / `✓ In Review`（英文，对齐底部 Review tab，明确收录去向），再点一下即从错题本移除。AI 自动收录的 gap 现在也回传 `reviewItemId`（`POST /api/correct` 把 id 回写进 gap，`POST /api/review-items` 返回 `ids` 列表），所以自动收录的同样能取消。

- 时间：2026-06-19 11:04（北京，秒取提交时间）
