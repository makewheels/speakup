# Changelog

`CHANGELOG.md` 是全仓库唯一 changelog：北京时间倒序分组，每条一行、前缀标类型（`add` / `change` / `fix` / `test` / `chore`）。

格式不僵硬，按改动大小自己拿捏：

- 小改动一行写清楚即可，不需要凑「背景 / 权衡」。
- 大功能可以在条目下自然补几句根因、权衡、验证结果，没有固定模板。
- 只增不改。后续修正推翻旧决策时写新条目并注明旧条目（如 2026-08-19 08:50 的自动笔记，在 08-23 19:52 段注明改为手动）。
