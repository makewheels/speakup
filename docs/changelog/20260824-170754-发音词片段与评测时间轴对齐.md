# fix：发音词片段与评测时间轴对齐

- 时间：2026-08-24 17:07:54（北京）

修复结果页点击低分词却播放到其他词的问题。腾讯词级时间戳来自服务端规范化后的 16k WAV，旧实现却让浏览器在原始 WebM/MP4 完整录音上异步 seek，二次单词评测也重新从原始容器裁切；容器时间轴与非精确 seek 都可能造成错位。

现在第一次评测、低分词二次评测和用户回听统一使用同一份规范化 WAV 时间轴。结果页通过鉴权接口取得已经裁好的短 WAV，不再由浏览器临时定位完整录音；分享页仅在分享仍开启时提供片段，片段即时生成且不永久保存。

发音卡同步简化为「你读成 / 目标音」两列，并把两个图标改为「我的录音 / 标准音」具名按钮；发音提示根据结构化音素和重音差异在前端本地化，不再在英文界面混入中文供应商解释。

验证：后端 302 项测试通过且覆盖率 88.83%；前端 311 项测试通过，语句、分支、函数、行覆盖率分别为 88.80%、80.89%、88.50%、92.62%；静态检查、源码行数门禁与生产构建通过。
