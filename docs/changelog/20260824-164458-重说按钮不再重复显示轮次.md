# fix：重说按钮不再重复显示轮次

- 时间：2026-08-24 16:44:58（北京）

结果页底部按钮从「Say it again (attempt N) / 再说一遍（第 N 次）」简化为固定的「Say it again / 再说一遍」。当前轮次已经在 IELTS 分数下方以 `Attempt #N` 徽章展示，按钮重复预告下一轮会增加理解成本。

重说不封顶、当前轮次计算和顶部徽章均保持不变。回归测试覆盖第二轮结果页仍显示 `Attempt #2`，同时按钮不再包含次数。

验证：前端 309 项测试、覆盖率门禁、静态检查和生产构建通过。
