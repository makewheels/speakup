# env 命名按能力解耦、不再绑运营商——`CHAT_*`（文字 LLM）/ `IMAGE_*`（图片）/ `VOICE_*`（ASR+TTS）三组，各带独立 key + base_url，将来换 Deepseek / 别家只改 `.env` 值不动代码。删除旧的 `DASHSCOPE_*` 变量名。`corrector.py`/`scenario_service.py` 走 `CHAT_*`，`wanx.py` 走 `IMAGE_*`，`transcriber.py`/`tts.py` 走 `VOICE_*`。

- 时间：2026-06-20 10:39（北京，秒取提交时间）
