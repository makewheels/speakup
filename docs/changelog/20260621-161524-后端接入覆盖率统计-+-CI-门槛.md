# **后端接入覆盖率统计 + CI 门槛**（原来只有前端有）。加 `pytest-cov`，`pytest` 默认带 `--cov --cov-fail-under=80`；纯外部 IO 适配器（wanx 文生图 / transcriber ASR，测试里整个 mock）从统计 omit，门槛守护真正的业务逻辑。后端整体 **84.67%**。

- 时间：2026-06-21 16:14（北京，秒取提交时间）
