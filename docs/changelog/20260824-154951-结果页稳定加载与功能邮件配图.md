# fix：结果页稳定加载与功能邮件配图

- 时间：2026-08-24 15:49:51（北京）

结果页删除了进入后 1.2 秒内反复滚动、以及流式完成后再次触发滚动的逻辑。IELTS 分数改为结果页第一项，加载阶段用同高占位保留空间，真实分数在原位更新；轮次放在分数下方。「分享本次结果」移到全部反馈、追问和下一步操作之后，成为页面最后一个区块。

功能完成邮件新增可选界面截图：工作流只接受仓库 `docs/assets/feature-notifications` 目录内不超过 2 MB 的 PNG/JPG，Resend 与 SMTP 都用 CID 内嵌附件发送，不依赖外部图片地址。截图路径限制、文件签名、大小和替代文字均在发送前校验；本次同时加入用测试数据生成的结果页截图。

验证：邮件脚本 12 项测试、前端 307 项测试和静态检查通过；生产构建通过；iPhone 13 浏览器回归确认等待 1.35 秒后滚动位置不变、分数位于轮次上方、分享入口是结果页最后一个节点。
