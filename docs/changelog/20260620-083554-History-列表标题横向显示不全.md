# History 列表标题横向显示不全。之前时间（`history-meta`）和标题在同一行抢横向空间，长标题被截断。把时间挪到标题下方（与 attempts / gaps chip 同一 `history-sub` 行），标题独占整行宽度；同时 `history-headline` 允许换到两行（`line-clamp` 1→2），长标题展示更完整。删掉空的 `.history-meta` 样式。

- 时间：2026-06-20 08:33（北京，秒取提交时间）
