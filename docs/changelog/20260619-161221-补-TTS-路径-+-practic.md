# 补 TTS 路径 + practiceId 透传 + tts.js 关键 bug 回归测试（后端 +6 / 前端 +7）。覆盖 `_cache_key` session/全局两分支、路由 practiceId 透传、tts.js 不阻塞 play 与 30s 超时、urlCache 按 (practiceId, text) 分键、空文本不调后端、SpeakBtn practiceId prop 透传。

- 时间：2026-06-19 15:58（北京，秒取提交时间）
