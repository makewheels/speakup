# 结果页表达建议与发音评测上线

- 时间：2026-08-24 12:40:36（北京）

结果页移除与 gaps 重复的整段/逐句纠正版。`gaps[]` 成为唯一纠正主体，最多 3 条；每条可以覆盖短语或完整句子，把用户原话放上方、同范围改写放下方，同一句中的相关问题尽量合并。新 attempt 不再生成或保存 `nativeVersion`、`sentenceCorrections`，历史旧字段保留但不展示。

结果页统一「表达建议 / 发音建议 / 标准答案 / 追问教练」标题层级。标准答案播放按钮移出折叠触发区，播放不会再收起内容；AI 结果反馈默认收成一个按钮，点击才展开，提交后自动收起。手机 390px 视口实测无横向溢出，整句 gap 可正常换行。

腾讯云口语评测（新版）已启用生产配置。应用使用无控制台登录的独立服务身份，只授予口语评测 WebSocket 调用权限；真实合成英文录音通过完整评测并返回 `completed`。总分优先采用供应商建议的 `SuggestedScore`，文字结果仍先到达，发音失败不影响表达反馈。

验证：后端 286 项测试通过且覆盖率 88.66%；前端 293 项测试、覆盖率门禁、静态检查与生产构建通过；补充了播放不折叠和反馈按需展开的交互回归测试。
