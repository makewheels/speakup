# 运营通知：注册与练习提交合并推送飞书

- 时间：2026-09-18 21:39:26（北京）
- 关联 PR：#206

注册与录音提交评估各产生一条事件，后台按窗口合并后推飞书，每 10 分钟最多一条消息。

- 事件源：登录新建用户分支、`_save_attempt_and_review` 收口（同步与流式评估共用）
- 首条立即发，窗口内后续事件攒到窗口结束合并；队列落 `notificationEvents`，部署重启不丢
- 开关 `NOTIFY_ENABLED` 默认关闭，webhook 与开关值走 Infisical `speakup/prod/notifications`
- 测试号不入队，手机号脱敏，失败重试超 5 次标 failed，日志不出现 webhook URL

验证：后端 432 项、前端 339 项、覆盖率、构建与行数门禁通过；本地假接收器跑通合并链路，
生产真发一条待 webhook 录入后执行。
