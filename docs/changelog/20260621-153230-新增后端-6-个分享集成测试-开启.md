# 新增后端 6 个分享集成测试（开启/幂等/撤销失效/非 owner 404/无效 token 404/sharedOnly 过滤）+ 前端 SharePage / ManageSharesPage / `lib/share` 测试，详情页测试更新为 tab 切换断言。修 `SessionDetailPage` 在 `getPractice` 返回 null 时读取 `s.shared` 报错。本地端到端实测（隐身窗口打开分享链接、撤销后 404）通过，后端 86 / 前端 114 全绿，build 与覆盖率门槛通过。

- 时间：2026-06-21 15:31（北京，秒取提交时间）
