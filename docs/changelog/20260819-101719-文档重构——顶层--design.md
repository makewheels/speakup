# 文档重构——顶层 `design/` 归入 `docs/原型设计/`；`docs/design/` 按业务模块分组（场景练习/）；新增 `docs/评测/`（evals + 发音纠正选型调研）、`docs/changelog/`（追加式变更档案：背景/权衡/验证，只增不改）、`docs/脚本说明.md`（两处 scripts/ 用途）

- 时间：2026-08-19 10:16（北京，秒取提交时间）
