# `docker-compose.yml` + CI「Write server .env」同步改用新变量；新增 GitHub Secret `CHAT_API_KEY`（火山 key），图片/语音复用现有 `DASHSCOPE_API_KEY` secret 值写入 prod 的 `IMAGE_API_KEY`/`VOICE_API_KEY`。`llm_audit` 价表加 `glm-5.2`（订阅制记 0）。

- 时间：2026-06-20 10:39（北京，秒取提交时间）
