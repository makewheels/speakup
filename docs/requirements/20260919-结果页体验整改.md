# 结果页体验整改

- 状态：已完成
- 关联 PR：#210

## 背景

结果页信息密集。产品所有者实机走查后提出 4 条改动，要求逐条落实、不遗漏。

## 改动清单

| # | 需求 | 说明 | 状态 |
| --- | --- | --- | --- |
| 1 | 删掉 gap 卡片的「See it in a different situation」 | 折叠例句块整体删除；调用大模型的提示词里 `example` / `exampleChinese` 的生成要求一并删除 | 已完成 |
| 2 | 「Say it again」与「Ask the coach」之间加分隔 | 两块现在紧贴、中间没有任何分割线，被误读成同一区块；要拉开距离并加分割线 | 已完成 |
| 3 | 轮次徽章第一次不显示 | 结果页顶部的「Attempt 1」只在第 2 次及以后显示——页面已经够复杂，首轮不必占位 | 已完成 |
| 4 | 结果页补「You said」区块 | 用户原话目前只在评估加载态出现，加载完就消失；要常驻结果页，默认折叠，并能播放原声 | 已完成 |

## 非目标

- 不改评分、gaps 生成、追问等主逻辑，只动呈现与提示词中已废弃的字段要求。

## 验收

- [x] 四条逐一完成（PR #210），实机走查确认
- [x] 改动完成后按批写 changelog（`20260919-125528-feat-结果页体验整改四条.md`）
