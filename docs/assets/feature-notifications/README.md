# 功能通知说明图

本目录只存放随功能完成邮件发送的 SVG 说明图。工作流仅接受可审查的 SVG，单图不超过 2 MB；图片以内嵌附件发送，不依赖外部 URL。

仓库禁止提交 PNG、JPG、GIF、WebP、HEIC 等栅格截图。需要真实界面截图时，必须在操作系统临时目录生成，只用于当次验收或邮件附件，发送后清理，不进入 Git。

SVG 应使用测试数据，不能包含真实手机号、用户内容、密钥、内部地址、脚本、外部资源引用或其他敏感信息。
